import ChatPanel from "./components/ChatPanel";

export default function App() {
  return (
    <main className="min-h-screen bg-linen text-cedar">
      <div className="mx-auto min-h-screen w-full max-w-[375px] bg-linen">
        <ChatPanel />
      </div>
    </main>
  );
}
